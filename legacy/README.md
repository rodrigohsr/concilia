Versao anterior do programa, mantida apenas para consulta.

Nao use estes arquivos: `app-desktop.py` importava `ofx_parser`, mas o arquivo
ao lado chamava-se `ofx-parser.py` (com hifen), entao o programa nao chegava a
abrir. A versao em uso esta na pasta acima.
